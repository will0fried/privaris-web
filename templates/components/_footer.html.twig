<footer class="site-foot">
    <div class="wrap">
        <div class="site-foot__top">
            <div>
                <a href="{{ path('app_home') }}" class="site-brand" aria-label="Privaris">
                    <svg viewBox="0 0 64 64" fill="none" stroke="currentColor" stroke-linecap="round" aria-hidden="true">
                        <circle cx="32" cy="32" r="22" stroke-width="1.4" opacity=".4"/>
                        <circle cx="32" cy="32" r="13" stroke-width="1.4" opacity=".6"/>
                        <circle cx="32" cy="32" r="5" stroke-width="2"/>
                        <circle cx="44" cy="22" r="2.4" fill="#E5484D" stroke="none"/>
                    </svg>
                    PRIVARIS
                </a>
                <p class="mono" style="color:var(--color-dim);font-size:12px;letter-spacing:.06em;margin:14px 0 0;">apprendre la cyber en public</p>
            </div>

            <div class="site-foot__cols">
                <div class="site-foot__col">
                    <h4>Contenu</h4>
                    <a href="{{ path('app_blog_index') }}">Journal</a>
                    <a href="{{ path('app_search') }}">Rechercher</a>
                </div>
                <div class="site-foot__col">
                    <h4>Site</h4>
                    <a href="{{ path('app_about') }}">À propos</a>
                    <a href="{{ path('app_contact') }}">Contact</a>
                    <a href="#newsletter">Newsletter</a>
                </div>
                <div class="site-foot__col">
                    <h4>Légal</h4>
                    <a href="{{ path('app_legal') }}">Mentions légales</a>
                    <a href="{{ path('app_privacy') }}">Confidentialité</a>
                </div>
            </div>
        </div>

        <div class="site-foot__base">
            <span>© {{ "now"|date("Y") }} Privaris · signé W.</span>
            <span>zéro pub · zéro traceur · <a href="{{ url('app_podcast_feed') }}" style="color:var(--color-dim);">RSS</a></span>
        </div>
    </div>
</footer>
