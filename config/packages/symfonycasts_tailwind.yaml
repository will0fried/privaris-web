symfonycasts_tailwind:
    # The path to the entry CSS file (relative to project dir)
    input_css: 'assets/styles/app.css'
    # tailwind.config.js est auto-détecté à la racine du projet
    # Version figée = plus d'appel à l'API GitHub (qui renvoyait 504).
    # Le binaire v4.3.1 est déjà présent dans var/tailwind/.
    binary_version: 'v4.3.1'
